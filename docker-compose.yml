services:
  web:
    extends:
      file: docker-compose.base.yml
      service: web
    image: ghcr.io/chiballc/manyumba
    
  db:
    extends:
      file: docker-compose.base.yml
      service: db

  watchtower:
    extends:
      file: docker-compose.base.yml
      service: watchtower

volumes:
  postgres_data: